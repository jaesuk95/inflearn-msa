spring:
  cloud:
    config:
      #      config server port number : 8888
      uri: http://127.0.0.1:8888
      #      ecommerce.yml (ex. application.yml)
      name: ecommerce
  profiles:
    active:
#    active: dev
